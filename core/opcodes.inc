// generated by tools/gen_opcodes.py from tools/Opcodes.json -- do not edit

uint32_t Cpu::execute_opcode(uint8_t opcode) {
    switch (opcode) {
    case 0x00: // nop
        return 4;
    case 0x01: // ld bc, n16
        set_bc(fetch16());
        return 12;
    case 0x02: // ld (bc), a
        bus_.write8(bc(), regs_.a);
        return 8;
    case 0x03: // inc bc
        set_bc(static_cast<uint16_t>(bc() + 1));
        return 8;
    case 0x04: { // inc b
        const AluResult r = alu_inc(regs_.b, regs_.f);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x05: { // dec b
        const AluResult r = alu_dec(regs_.b, regs_.f);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x06: // ld b, n8
        regs_.b = fetch8();
        return 8;
    case 0x07: { // rlca
        const AluResult r = alu_rlc(regs_.a);
        regs_.a = r.value;
        regs_.f = static_cast<uint8_t>(r.flags & ~kFlagZ);
        return 4;
    }
    case 0x08: { // ld (a16), sp
        const uint16_t addr = fetch16();
        bus_.write8(addr, static_cast<uint8_t>(sp() & 0xFF));
        bus_.write8(static_cast<uint16_t>(addr + 1), static_cast<uint8_t>(sp() >> 8));
        return 20;
    }
    case 0x09: { // add hl, bc
        const Alu16Result r = alu_add16(hl(), bc(), regs_.f);
        set_hl(r.value);
        regs_.f = r.flags;
        return 8;
    }
    case 0x0A: // ld a, (bc)
        regs_.a = bus_.read8(bc());
        return 8;
    case 0x0B: // dec bc
        set_bc(static_cast<uint16_t>(bc() - 1));
        return 8;
    case 0x0C: { // inc c
        const AluResult r = alu_inc(regs_.c, regs_.f);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x0D: { // dec c
        const AluResult r = alu_dec(regs_.c, regs_.f);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x0E: // ld c, n8
        regs_.c = fetch8();
        return 8;
    case 0x0F: { // rrca
        const AluResult r = alu_rrc(regs_.a);
        regs_.a = r.value;
        regs_.f = static_cast<uint8_t>(r.flags & ~kFlagZ);
        return 4;
    }
    case 0x10: // stop n8
        return do_stop();
    case 0x11: // ld de, n16
        set_de(fetch16());
        return 12;
    case 0x12: // ld (de), a
        bus_.write8(de(), regs_.a);
        return 8;
    case 0x13: // inc de
        set_de(static_cast<uint16_t>(de() + 1));
        return 8;
    case 0x14: { // inc d
        const AluResult r = alu_inc(regs_.d, regs_.f);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x15: { // dec d
        const AluResult r = alu_dec(regs_.d, regs_.f);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x16: // ld d, n8
        regs_.d = fetch8();
        return 8;
    case 0x17: { // rla
        const AluResult r = alu_rl(regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = static_cast<uint8_t>(r.flags & ~kFlagZ);
        return 4;
    }
    case 0x18: { // jr e8
        const int8_t off = static_cast<int8_t>(fetch8());
        regs_.pc = static_cast<uint16_t>(regs_.pc + off);
        return 12;
    }
    case 0x19: { // add hl, de
        const Alu16Result r = alu_add16(hl(), de(), regs_.f);
        set_hl(r.value);
        regs_.f = r.flags;
        return 8;
    }
    case 0x1A: // ld a, (de)
        regs_.a = bus_.read8(de());
        return 8;
    case 0x1B: // dec de
        set_de(static_cast<uint16_t>(de() - 1));
        return 8;
    case 0x1C: { // inc e
        const AluResult r = alu_inc(regs_.e, regs_.f);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x1D: { // dec e
        const AluResult r = alu_dec(regs_.e, regs_.f);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x1E: // ld e, n8
        regs_.e = fetch8();
        return 8;
    case 0x1F: { // rra
        const AluResult r = alu_rr(regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = static_cast<uint8_t>(r.flags & ~kFlagZ);
        return 4;
    }
    case 0x20: { // jr nz, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        if (!flag_z()) {
            regs_.pc = static_cast<uint16_t>(regs_.pc + off);
            return 12;
        }
        return 8;
    }
    case 0x21: // ld hl, n16
        set_hl(fetch16());
        return 12;
    case 0x22: // ld (hl+), a
        bus_.write8(hl(), regs_.a);
        set_hl(static_cast<uint16_t>(hl() + 1));
        return 8;
    case 0x23: // inc hl
        set_hl(static_cast<uint16_t>(hl() + 1));
        return 8;
    case 0x24: { // inc h
        const AluResult r = alu_inc(regs_.h, regs_.f);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x25: { // dec h
        const AluResult r = alu_dec(regs_.h, regs_.f);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x26: // ld h, n8
        regs_.h = fetch8();
        return 8;
    case 0x27: { // daa
        const AluResult r = alu_daa(regs_.a, regs_.f);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x28: { // jr z, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        if (flag_z()) {
            regs_.pc = static_cast<uint16_t>(regs_.pc + off);
            return 12;
        }
        return 8;
    }
    case 0x29: { // add hl, hl
        const Alu16Result r = alu_add16(hl(), hl(), regs_.f);
        set_hl(r.value);
        regs_.f = r.flags;
        return 8;
    }
    case 0x2A: { // ld a, (hl+)
        const uint8_t m = bus_.read8(hl());
        set_hl(static_cast<uint16_t>(hl() + 1));
        regs_.a = m;
        return 8;
    }
    case 0x2B: // dec hl
        set_hl(static_cast<uint16_t>(hl() - 1));
        return 8;
    case 0x2C: { // inc l
        const AluResult r = alu_inc(regs_.l, regs_.f);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x2D: { // dec l
        const AluResult r = alu_dec(regs_.l, regs_.f);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x2E: // ld l, n8
        regs_.l = fetch8();
        return 8;
    case 0x2F: // cpl
        regs_.a = static_cast<uint8_t>(~regs_.a);
        regs_.f = static_cast<uint8_t>((regs_.f & (kFlagZ | kFlagC)) | kFlagN | kFlagH);
        return 4;
    case 0x30: { // jr nc, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        if (!flag_c()) {
            regs_.pc = static_cast<uint16_t>(regs_.pc + off);
            return 12;
        }
        return 8;
    }
    case 0x31: // ld sp, n16
        regs_.sp = fetch16();
        return 12;
    case 0x32: // ld (hl-), a
        bus_.write8(hl(), regs_.a);
        set_hl(static_cast<uint16_t>(hl() - 1));
        return 8;
    case 0x33: // inc sp
        regs_.sp = static_cast<uint16_t>(sp() + 1);
        return 8;
    case 0x34: { // inc (hl)
        const AluResult r = alu_inc(bus_.read8(hl()), regs_.f);
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 12;
    }
    case 0x35: { // dec (hl)
        const AluResult r = alu_dec(bus_.read8(hl()), regs_.f);
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 12;
    }
    case 0x36: // ld (hl), n8
        bus_.write8(hl(), fetch8());
        return 12;
    case 0x37: // scf
        regs_.f = static_cast<uint8_t>((regs_.f & kFlagZ) | kFlagC);
        return 4;
    case 0x38: { // jr c, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        if (flag_c()) {
            regs_.pc = static_cast<uint16_t>(regs_.pc + off);
            return 12;
        }
        return 8;
    }
    case 0x39: { // add hl, sp
        const Alu16Result r = alu_add16(hl(), sp(), regs_.f);
        set_hl(r.value);
        regs_.f = r.flags;
        return 8;
    }
    case 0x3A: { // ld a, (hl-)
        const uint8_t m = bus_.read8(hl());
        set_hl(static_cast<uint16_t>(hl() - 1));
        regs_.a = m;
        return 8;
    }
    case 0x3B: // dec sp
        regs_.sp = static_cast<uint16_t>(sp() - 1);
        return 8;
    case 0x3C: { // inc a
        const AluResult r = alu_inc(regs_.a, regs_.f);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x3D: { // dec a
        const AluResult r = alu_dec(regs_.a, regs_.f);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x3E: // ld a, n8
        regs_.a = fetch8();
        return 8;
    case 0x3F: // ccf
        regs_.f = static_cast<uint8_t>((regs_.f & kFlagZ) | ((regs_.f ^ kFlagC) & kFlagC));
        return 4;
    case 0x40: // ld b, b
        regs_.b = regs_.b;
        return 4;
    case 0x41: // ld b, c
        regs_.b = regs_.c;
        return 4;
    case 0x42: // ld b, d
        regs_.b = regs_.d;
        return 4;
    case 0x43: // ld b, e
        regs_.b = regs_.e;
        return 4;
    case 0x44: // ld b, h
        regs_.b = regs_.h;
        return 4;
    case 0x45: // ld b, l
        regs_.b = regs_.l;
        return 4;
    case 0x46: // ld b, (hl)
        regs_.b = bus_.read8(hl());
        return 8;
    case 0x47: // ld b, a
        regs_.b = regs_.a;
        return 4;
    case 0x48: // ld c, b
        regs_.c = regs_.b;
        return 4;
    case 0x49: // ld c, c
        regs_.c = regs_.c;
        return 4;
    case 0x4A: // ld c, d
        regs_.c = regs_.d;
        return 4;
    case 0x4B: // ld c, e
        regs_.c = regs_.e;
        return 4;
    case 0x4C: // ld c, h
        regs_.c = regs_.h;
        return 4;
    case 0x4D: // ld c, l
        regs_.c = regs_.l;
        return 4;
    case 0x4E: // ld c, (hl)
        regs_.c = bus_.read8(hl());
        return 8;
    case 0x4F: // ld c, a
        regs_.c = regs_.a;
        return 4;
    case 0x50: // ld d, b
        regs_.d = regs_.b;
        return 4;
    case 0x51: // ld d, c
        regs_.d = regs_.c;
        return 4;
    case 0x52: // ld d, d
        regs_.d = regs_.d;
        return 4;
    case 0x53: // ld d, e
        regs_.d = regs_.e;
        return 4;
    case 0x54: // ld d, h
        regs_.d = regs_.h;
        return 4;
    case 0x55: // ld d, l
        regs_.d = regs_.l;
        return 4;
    case 0x56: // ld d, (hl)
        regs_.d = bus_.read8(hl());
        return 8;
    case 0x57: // ld d, a
        regs_.d = regs_.a;
        return 4;
    case 0x58: // ld e, b
        regs_.e = regs_.b;
        return 4;
    case 0x59: // ld e, c
        regs_.e = regs_.c;
        return 4;
    case 0x5A: // ld e, d
        regs_.e = regs_.d;
        return 4;
    case 0x5B: // ld e, e
        regs_.e = regs_.e;
        return 4;
    case 0x5C: // ld e, h
        regs_.e = regs_.h;
        return 4;
    case 0x5D: // ld e, l
        regs_.e = regs_.l;
        return 4;
    case 0x5E: // ld e, (hl)
        regs_.e = bus_.read8(hl());
        return 8;
    case 0x5F: // ld e, a
        regs_.e = regs_.a;
        return 4;
    case 0x60: // ld h, b
        regs_.h = regs_.b;
        return 4;
    case 0x61: // ld h, c
        regs_.h = regs_.c;
        return 4;
    case 0x62: // ld h, d
        regs_.h = regs_.d;
        return 4;
    case 0x63: // ld h, e
        regs_.h = regs_.e;
        return 4;
    case 0x64: // ld h, h
        regs_.h = regs_.h;
        return 4;
    case 0x65: // ld h, l
        regs_.h = regs_.l;
        return 4;
    case 0x66: // ld h, (hl)
        regs_.h = bus_.read8(hl());
        return 8;
    case 0x67: // ld h, a
        regs_.h = regs_.a;
        return 4;
    case 0x68: // ld l, b
        regs_.l = regs_.b;
        return 4;
    case 0x69: // ld l, c
        regs_.l = regs_.c;
        return 4;
    case 0x6A: // ld l, d
        regs_.l = regs_.d;
        return 4;
    case 0x6B: // ld l, e
        regs_.l = regs_.e;
        return 4;
    case 0x6C: // ld l, h
        regs_.l = regs_.h;
        return 4;
    case 0x6D: // ld l, l
        regs_.l = regs_.l;
        return 4;
    case 0x6E: // ld l, (hl)
        regs_.l = bus_.read8(hl());
        return 8;
    case 0x6F: // ld l, a
        regs_.l = regs_.a;
        return 4;
    case 0x70: // ld (hl), b
        bus_.write8(hl(), regs_.b);
        return 8;
    case 0x71: // ld (hl), c
        bus_.write8(hl(), regs_.c);
        return 8;
    case 0x72: // ld (hl), d
        bus_.write8(hl(), regs_.d);
        return 8;
    case 0x73: // ld (hl), e
        bus_.write8(hl(), regs_.e);
        return 8;
    case 0x74: // ld (hl), h
        bus_.write8(hl(), regs_.h);
        return 8;
    case 0x75: // ld (hl), l
        bus_.write8(hl(), regs_.l);
        return 8;
    case 0x76: // halt
        return do_halt();
    case 0x77: // ld (hl), a
        bus_.write8(hl(), regs_.a);
        return 8;
    case 0x78: // ld a, b
        regs_.a = regs_.b;
        return 4;
    case 0x79: // ld a, c
        regs_.a = regs_.c;
        return 4;
    case 0x7A: // ld a, d
        regs_.a = regs_.d;
        return 4;
    case 0x7B: // ld a, e
        regs_.a = regs_.e;
        return 4;
    case 0x7C: // ld a, h
        regs_.a = regs_.h;
        return 4;
    case 0x7D: // ld a, l
        regs_.a = regs_.l;
        return 4;
    case 0x7E: // ld a, (hl)
        regs_.a = bus_.read8(hl());
        return 8;
    case 0x7F: // ld a, a
        regs_.a = regs_.a;
        return 4;
    case 0x80: { // add a, b
        const AluResult r = alu_add(regs_.a, regs_.b);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x81: { // add a, c
        const AluResult r = alu_add(regs_.a, regs_.c);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x82: { // add a, d
        const AluResult r = alu_add(regs_.a, regs_.d);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x83: { // add a, e
        const AluResult r = alu_add(regs_.a, regs_.e);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x84: { // add a, h
        const AluResult r = alu_add(regs_.a, regs_.h);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x85: { // add a, l
        const AluResult r = alu_add(regs_.a, regs_.l);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x86: { // add a, (hl)
        const AluResult r = alu_add(regs_.a, bus_.read8(hl()));
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x87: { // add a, a
        const AluResult r = alu_add(regs_.a, regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x88: { // adc a, b
        const AluResult r = alu_adc(regs_.a, regs_.b, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x89: { // adc a, c
        const AluResult r = alu_adc(regs_.a, regs_.c, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x8A: { // adc a, d
        const AluResult r = alu_adc(regs_.a, regs_.d, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x8B: { // adc a, e
        const AluResult r = alu_adc(regs_.a, regs_.e, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x8C: { // adc a, h
        const AluResult r = alu_adc(regs_.a, regs_.h, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x8D: { // adc a, l
        const AluResult r = alu_adc(regs_.a, regs_.l, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x8E: { // adc a, (hl)
        const AluResult r = alu_adc(regs_.a, bus_.read8(hl()), flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x8F: { // adc a, a
        const AluResult r = alu_adc(regs_.a, regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x90: { // sub a, b
        const AluResult r = alu_sub(regs_.a, regs_.b);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x91: { // sub a, c
        const AluResult r = alu_sub(regs_.a, regs_.c);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x92: { // sub a, d
        const AluResult r = alu_sub(regs_.a, regs_.d);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x93: { // sub a, e
        const AluResult r = alu_sub(regs_.a, regs_.e);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x94: { // sub a, h
        const AluResult r = alu_sub(regs_.a, regs_.h);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x95: { // sub a, l
        const AluResult r = alu_sub(regs_.a, regs_.l);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x96: { // sub a, (hl)
        const AluResult r = alu_sub(regs_.a, bus_.read8(hl()));
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x97: { // sub a, a
        const AluResult r = alu_sub(regs_.a, regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x98: { // sbc a, b
        const AluResult r = alu_sbc(regs_.a, regs_.b, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x99: { // sbc a, c
        const AluResult r = alu_sbc(regs_.a, regs_.c, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x9A: { // sbc a, d
        const AluResult r = alu_sbc(regs_.a, regs_.d, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x9B: { // sbc a, e
        const AluResult r = alu_sbc(regs_.a, regs_.e, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x9C: { // sbc a, h
        const AluResult r = alu_sbc(regs_.a, regs_.h, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x9D: { // sbc a, l
        const AluResult r = alu_sbc(regs_.a, regs_.l, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0x9E: { // sbc a, (hl)
        const AluResult r = alu_sbc(regs_.a, bus_.read8(hl()), flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x9F: { // sbc a, a
        const AluResult r = alu_sbc(regs_.a, regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA0: { // and a, b
        const AluResult r = alu_and(regs_.a, regs_.b);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA1: { // and a, c
        const AluResult r = alu_and(regs_.a, regs_.c);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA2: { // and a, d
        const AluResult r = alu_and(regs_.a, regs_.d);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA3: { // and a, e
        const AluResult r = alu_and(regs_.a, regs_.e);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA4: { // and a, h
        const AluResult r = alu_and(regs_.a, regs_.h);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA5: { // and a, l
        const AluResult r = alu_and(regs_.a, regs_.l);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA6: { // and a, (hl)
        const AluResult r = alu_and(regs_.a, bus_.read8(hl()));
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xA7: { // and a, a
        const AluResult r = alu_and(regs_.a, regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA8: { // xor a, b
        const AluResult r = alu_xor(regs_.a, regs_.b);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xA9: { // xor a, c
        const AluResult r = alu_xor(regs_.a, regs_.c);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xAA: { // xor a, d
        const AluResult r = alu_xor(regs_.a, regs_.d);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xAB: { // xor a, e
        const AluResult r = alu_xor(regs_.a, regs_.e);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xAC: { // xor a, h
        const AluResult r = alu_xor(regs_.a, regs_.h);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xAD: { // xor a, l
        const AluResult r = alu_xor(regs_.a, regs_.l);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xAE: { // xor a, (hl)
        const AluResult r = alu_xor(regs_.a, bus_.read8(hl()));
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xAF: { // xor a, a
        const AluResult r = alu_xor(regs_.a, regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB0: { // or a, b
        const AluResult r = alu_or(regs_.a, regs_.b);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB1: { // or a, c
        const AluResult r = alu_or(regs_.a, regs_.c);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB2: { // or a, d
        const AluResult r = alu_or(regs_.a, regs_.d);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB3: { // or a, e
        const AluResult r = alu_or(regs_.a, regs_.e);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB4: { // or a, h
        const AluResult r = alu_or(regs_.a, regs_.h);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB5: { // or a, l
        const AluResult r = alu_or(regs_.a, regs_.l);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB6: { // or a, (hl)
        const AluResult r = alu_or(regs_.a, bus_.read8(hl()));
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xB7: { // or a, a
        const AluResult r = alu_or(regs_.a, regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 4;
    }
    case 0xB8: // cp a, b
        regs_.f = alu_sub(regs_.a, regs_.b).flags;
        return 4;
    case 0xB9: // cp a, c
        regs_.f = alu_sub(regs_.a, regs_.c).flags;
        return 4;
    case 0xBA: // cp a, d
        regs_.f = alu_sub(regs_.a, regs_.d).flags;
        return 4;
    case 0xBB: // cp a, e
        regs_.f = alu_sub(regs_.a, regs_.e).flags;
        return 4;
    case 0xBC: // cp a, h
        regs_.f = alu_sub(regs_.a, regs_.h).flags;
        return 4;
    case 0xBD: // cp a, l
        regs_.f = alu_sub(regs_.a, regs_.l).flags;
        return 4;
    case 0xBE: // cp a, (hl)
        regs_.f = alu_sub(regs_.a, bus_.read8(hl())).flags;
        return 8;
    case 0xBF: // cp a, a
        regs_.f = alu_sub(regs_.a, regs_.a).flags;
        return 4;
    case 0xC0: { // ret nz
        if (!flag_z()) {
            regs_.pc = pop16();
            return 20;
        }
        return 8;
    }
    case 0xC1: // pop bc
        set_bc(pop16());
        return 12;
    case 0xC2: { // jp nz, a16
        const uint16_t target = fetch16();
        if (!flag_z()) {
            regs_.pc = target;
            return 16;
        }
        return 12;
    }
    case 0xC3: { // jp a16
        const uint16_t target = fetch16();
        regs_.pc = target;
        return 16;
    }
    case 0xC4: { // call nz, a16
        const uint16_t target = fetch16();
        if (!flag_z()) {
            push16(regs_.pc);
            regs_.pc = target;
            return 24;
        }
        return 12;
    }
    case 0xC5: // push bc
        push16(bc());
        return 16;
    case 0xC6: { // add a, n8
        const AluResult r = alu_add(regs_.a, fetch8());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xC7: // rst $00
        push16(regs_.pc);
        regs_.pc = 0x0000;
        return 16;
    case 0xC8: { // ret z
        if (flag_z()) {
            regs_.pc = pop16();
            return 20;
        }
        return 8;
    }
    case 0xC9: // ret
        regs_.pc = pop16();
        return 16;
    case 0xCA: { // jp z, a16
        const uint16_t target = fetch16();
        if (flag_z()) {
            regs_.pc = target;
            return 16;
        }
        return 12;
    }
    case 0xCB: // prefix
        return execute_cb(fetch8());
    case 0xCC: { // call z, a16
        const uint16_t target = fetch16();
        if (flag_z()) {
            push16(regs_.pc);
            regs_.pc = target;
            return 24;
        }
        return 12;
    }
    case 0xCD: { // call a16
        const uint16_t target = fetch16();
        push16(regs_.pc);
        regs_.pc = target;
        return 24;
    }
    case 0xCE: { // adc a, n8
        const AluResult r = alu_adc(regs_.a, fetch8(), flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xCF: // rst $08
        push16(regs_.pc);
        regs_.pc = 0x0008;
        return 16;
    case 0xD0: { // ret nc
        if (!flag_c()) {
            regs_.pc = pop16();
            return 20;
        }
        return 8;
    }
    case 0xD1: // pop de
        set_de(pop16());
        return 12;
    case 0xD2: { // jp nc, a16
        const uint16_t target = fetch16();
        if (!flag_c()) {
            regs_.pc = target;
            return 16;
        }
        return 12;
    }
    case 0xD3: // illegal_d3
        return trap_unknown(opcode);
    case 0xD4: { // call nc, a16
        const uint16_t target = fetch16();
        if (!flag_c()) {
            push16(regs_.pc);
            regs_.pc = target;
            return 24;
        }
        return 12;
    }
    case 0xD5: // push de
        push16(de());
        return 16;
    case 0xD6: { // sub a, n8
        const AluResult r = alu_sub(regs_.a, fetch8());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xD7: // rst $10
        push16(regs_.pc);
        regs_.pc = 0x0010;
        return 16;
    case 0xD8: { // ret c
        if (flag_c()) {
            regs_.pc = pop16();
            return 20;
        }
        return 8;
    }
    case 0xD9: // reti
        regs_.pc = pop16();
        ime_ = true;
        ime_delay_ = 0;
        return 16;
    case 0xDA: { // jp c, a16
        const uint16_t target = fetch16();
        if (flag_c()) {
            regs_.pc = target;
            return 16;
        }
        return 12;
    }
    case 0xDB: // illegal_db
        return trap_unknown(opcode);
    case 0xDC: { // call c, a16
        const uint16_t target = fetch16();
        if (flag_c()) {
            push16(regs_.pc);
            regs_.pc = target;
            return 24;
        }
        return 12;
    }
    case 0xDD: // illegal_dd
        return trap_unknown(opcode);
    case 0xDE: { // sbc a, n8
        const AluResult r = alu_sbc(regs_.a, fetch8(), flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xDF: // rst $18
        push16(regs_.pc);
        regs_.pc = 0x0018;
        return 16;
    case 0xE0: // ldh (a8), a
        bus_.write8(static_cast<uint16_t>(0xFF00 | fetch8()), regs_.a);
        return 12;
    case 0xE1: // pop hl
        set_hl(pop16());
        return 12;
    case 0xE2: // ldh (c), a
        bus_.write8(static_cast<uint16_t>(0xFF00 | regs_.c), regs_.a);
        return 8;
    case 0xE3: // illegal_e3
        return trap_unknown(opcode);
    case 0xE4: // illegal_e4
        return trap_unknown(opcode);
    case 0xE5: // push hl
        push16(hl());
        return 16;
    case 0xE6: { // and a, n8
        const AluResult r = alu_and(regs_.a, fetch8());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xE7: // rst $20
        push16(regs_.pc);
        regs_.pc = 0x0020;
        return 16;
    case 0xE8: { // add sp, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        const Alu16Result r = alu_add_sp_e8(sp(), off);
        regs_.sp = r.value;
        regs_.f = r.flags;
        return 16;
    }
    case 0xE9: // jp hl
        regs_.pc = hl();
        return 4;
    case 0xEA: // ld (a16), a
        bus_.write8(fetch16(), regs_.a);
        return 16;
    case 0xEB: // illegal_eb
        return trap_unknown(opcode);
    case 0xEC: // illegal_ec
        return trap_unknown(opcode);
    case 0xED: // illegal_ed
        return trap_unknown(opcode);
    case 0xEE: { // xor a, n8
        const AluResult r = alu_xor(regs_.a, fetch8());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xEF: // rst $28
        push16(regs_.pc);
        regs_.pc = 0x0028;
        return 16;
    case 0xF0: // ldh a, (a8)
        regs_.a = bus_.read8(static_cast<uint16_t>(0xFF00 | fetch8()));
        return 12;
    case 0xF1: // pop af
        set_af(pop16());
        return 12;
    case 0xF2: // ldh a, (c)
        regs_.a = bus_.read8(static_cast<uint16_t>(0xFF00 | regs_.c));
        return 8;
    case 0xF3: // di
        ime_ = false;
        ime_delay_ = 0;
        return 4;
    case 0xF4: // illegal_f4
        return trap_unknown(opcode);
    case 0xF5: // push af
        push16(af());
        return 16;
    case 0xF6: { // or a, n8
        const AluResult r = alu_or(regs_.a, fetch8());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0xF7: // rst $30
        push16(regs_.pc);
        regs_.pc = 0x0030;
        return 16;
    case 0xF8: { // ld hl, sp+, e8
        const int8_t off = static_cast<int8_t>(fetch8());
        const Alu16Result r = alu_add_sp_e8(sp(), off);
        set_hl(r.value);
        regs_.f = r.flags;
        return 12;
    }
    case 0xF9: // ld sp, hl
        regs_.sp = hl();
        return 8;
    case 0xFA: // ld a, (a16)
        regs_.a = bus_.read8(fetch16());
        return 16;
    case 0xFB: // ei
        ime_delay_ = 2;
        return 4;
    case 0xFC: // illegal_fc
        return trap_unknown(opcode);
    case 0xFD: // illegal_fd
        return trap_unknown(opcode);
    case 0xFE: // cp a, n8
        regs_.f = alu_sub(regs_.a, fetch8()).flags;
        return 8;
    case 0xFF: // rst $38
        push16(regs_.pc);
        regs_.pc = 0x0038;
        return 16;
    }
    return trap_unknown(opcode);
}

uint32_t Cpu::execute_cb(uint8_t opcode) {
    switch (opcode) {
    case 0x00: { // rlc b
        const AluResult r = alu_rlc(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x01: { // rlc c
        const AluResult r = alu_rlc(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x02: { // rlc d
        const AluResult r = alu_rlc(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x03: { // rlc e
        const AluResult r = alu_rlc(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x04: { // rlc h
        const AluResult r = alu_rlc(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x05: { // rlc l
        const AluResult r = alu_rlc(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x06: { // rlc (hl)
        const AluResult r = alu_rlc(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x07: { // rlc a
        const AluResult r = alu_rlc(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x08: { // rrc b
        const AluResult r = alu_rrc(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x09: { // rrc c
        const AluResult r = alu_rrc(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x0A: { // rrc d
        const AluResult r = alu_rrc(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x0B: { // rrc e
        const AluResult r = alu_rrc(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x0C: { // rrc h
        const AluResult r = alu_rrc(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x0D: { // rrc l
        const AluResult r = alu_rrc(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x0E: { // rrc (hl)
        const AluResult r = alu_rrc(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x0F: { // rrc a
        const AluResult r = alu_rrc(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x10: { // rl b
        const AluResult r = alu_rl(regs_.b, flag_c());
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x11: { // rl c
        const AluResult r = alu_rl(regs_.c, flag_c());
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x12: { // rl d
        const AluResult r = alu_rl(regs_.d, flag_c());
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x13: { // rl e
        const AluResult r = alu_rl(regs_.e, flag_c());
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x14: { // rl h
        const AluResult r = alu_rl(regs_.h, flag_c());
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x15: { // rl l
        const AluResult r = alu_rl(regs_.l, flag_c());
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x16: { // rl (hl)
        const AluResult r = alu_rl(bus_.read8(hl()), flag_c());
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x17: { // rl a
        const AluResult r = alu_rl(regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x18: { // rr b
        const AluResult r = alu_rr(regs_.b, flag_c());
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x19: { // rr c
        const AluResult r = alu_rr(regs_.c, flag_c());
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x1A: { // rr d
        const AluResult r = alu_rr(regs_.d, flag_c());
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x1B: { // rr e
        const AluResult r = alu_rr(regs_.e, flag_c());
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x1C: { // rr h
        const AluResult r = alu_rr(regs_.h, flag_c());
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x1D: { // rr l
        const AluResult r = alu_rr(regs_.l, flag_c());
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x1E: { // rr (hl)
        const AluResult r = alu_rr(bus_.read8(hl()), flag_c());
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x1F: { // rr a
        const AluResult r = alu_rr(regs_.a, flag_c());
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x20: { // sla b
        const AluResult r = alu_sla(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x21: { // sla c
        const AluResult r = alu_sla(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x22: { // sla d
        const AluResult r = alu_sla(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x23: { // sla e
        const AluResult r = alu_sla(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x24: { // sla h
        const AluResult r = alu_sla(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x25: { // sla l
        const AluResult r = alu_sla(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x26: { // sla (hl)
        const AluResult r = alu_sla(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x27: { // sla a
        const AluResult r = alu_sla(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x28: { // sra b
        const AluResult r = alu_sra(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x29: { // sra c
        const AluResult r = alu_sra(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x2A: { // sra d
        const AluResult r = alu_sra(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x2B: { // sra e
        const AluResult r = alu_sra(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x2C: { // sra h
        const AluResult r = alu_sra(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x2D: { // sra l
        const AluResult r = alu_sra(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x2E: { // sra (hl)
        const AluResult r = alu_sra(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x2F: { // sra a
        const AluResult r = alu_sra(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x30: { // swap b
        const AluResult r = alu_swap(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x31: { // swap c
        const AluResult r = alu_swap(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x32: { // swap d
        const AluResult r = alu_swap(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x33: { // swap e
        const AluResult r = alu_swap(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x34: { // swap h
        const AluResult r = alu_swap(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x35: { // swap l
        const AluResult r = alu_swap(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x36: { // swap (hl)
        const AluResult r = alu_swap(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x37: { // swap a
        const AluResult r = alu_swap(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x38: { // srl b
        const AluResult r = alu_srl(regs_.b);
        regs_.b = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x39: { // srl c
        const AluResult r = alu_srl(regs_.c);
        regs_.c = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x3A: { // srl d
        const AluResult r = alu_srl(regs_.d);
        regs_.d = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x3B: { // srl e
        const AluResult r = alu_srl(regs_.e);
        regs_.e = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x3C: { // srl h
        const AluResult r = alu_srl(regs_.h);
        regs_.h = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x3D: { // srl l
        const AluResult r = alu_srl(regs_.l);
        regs_.l = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x3E: { // srl (hl)
        const AluResult r = alu_srl(bus_.read8(hl()));
        bus_.write8(hl(), r.value);
        regs_.f = r.flags;
        return 16;
    }
    case 0x3F: { // srl a
        const AluResult r = alu_srl(regs_.a);
        regs_.a = r.value;
        regs_.f = r.flags;
        return 8;
    }
    case 0x40: // bit 0, b
        regs_.f = alu_bit(regs_.b, 0, regs_.f);
        return 8;
    case 0x41: // bit 0, c
        regs_.f = alu_bit(regs_.c, 0, regs_.f);
        return 8;
    case 0x42: // bit 0, d
        regs_.f = alu_bit(regs_.d, 0, regs_.f);
        return 8;
    case 0x43: // bit 0, e
        regs_.f = alu_bit(regs_.e, 0, regs_.f);
        return 8;
    case 0x44: // bit 0, h
        regs_.f = alu_bit(regs_.h, 0, regs_.f);
        return 8;
    case 0x45: // bit 0, l
        regs_.f = alu_bit(regs_.l, 0, regs_.f);
        return 8;
    case 0x46: // bit 0, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 0, regs_.f);
        return 12;
    case 0x47: // bit 0, a
        regs_.f = alu_bit(regs_.a, 0, regs_.f);
        return 8;
    case 0x48: // bit 1, b
        regs_.f = alu_bit(regs_.b, 1, regs_.f);
        return 8;
    case 0x49: // bit 1, c
        regs_.f = alu_bit(regs_.c, 1, regs_.f);
        return 8;
    case 0x4A: // bit 1, d
        regs_.f = alu_bit(regs_.d, 1, regs_.f);
        return 8;
    case 0x4B: // bit 1, e
        regs_.f = alu_bit(regs_.e, 1, regs_.f);
        return 8;
    case 0x4C: // bit 1, h
        regs_.f = alu_bit(regs_.h, 1, regs_.f);
        return 8;
    case 0x4D: // bit 1, l
        regs_.f = alu_bit(regs_.l, 1, regs_.f);
        return 8;
    case 0x4E: // bit 1, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 1, regs_.f);
        return 12;
    case 0x4F: // bit 1, a
        regs_.f = alu_bit(regs_.a, 1, regs_.f);
        return 8;
    case 0x50: // bit 2, b
        regs_.f = alu_bit(regs_.b, 2, regs_.f);
        return 8;
    case 0x51: // bit 2, c
        regs_.f = alu_bit(regs_.c, 2, regs_.f);
        return 8;
    case 0x52: // bit 2, d
        regs_.f = alu_bit(regs_.d, 2, regs_.f);
        return 8;
    case 0x53: // bit 2, e
        regs_.f = alu_bit(regs_.e, 2, regs_.f);
        return 8;
    case 0x54: // bit 2, h
        regs_.f = alu_bit(regs_.h, 2, regs_.f);
        return 8;
    case 0x55: // bit 2, l
        regs_.f = alu_bit(regs_.l, 2, regs_.f);
        return 8;
    case 0x56: // bit 2, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 2, regs_.f);
        return 12;
    case 0x57: // bit 2, a
        regs_.f = alu_bit(regs_.a, 2, regs_.f);
        return 8;
    case 0x58: // bit 3, b
        regs_.f = alu_bit(regs_.b, 3, regs_.f);
        return 8;
    case 0x59: // bit 3, c
        regs_.f = alu_bit(regs_.c, 3, regs_.f);
        return 8;
    case 0x5A: // bit 3, d
        regs_.f = alu_bit(regs_.d, 3, regs_.f);
        return 8;
    case 0x5B: // bit 3, e
        regs_.f = alu_bit(regs_.e, 3, regs_.f);
        return 8;
    case 0x5C: // bit 3, h
        regs_.f = alu_bit(regs_.h, 3, regs_.f);
        return 8;
    case 0x5D: // bit 3, l
        regs_.f = alu_bit(regs_.l, 3, regs_.f);
        return 8;
    case 0x5E: // bit 3, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 3, regs_.f);
        return 12;
    case 0x5F: // bit 3, a
        regs_.f = alu_bit(regs_.a, 3, regs_.f);
        return 8;
    case 0x60: // bit 4, b
        regs_.f = alu_bit(regs_.b, 4, regs_.f);
        return 8;
    case 0x61: // bit 4, c
        regs_.f = alu_bit(regs_.c, 4, regs_.f);
        return 8;
    case 0x62: // bit 4, d
        regs_.f = alu_bit(regs_.d, 4, regs_.f);
        return 8;
    case 0x63: // bit 4, e
        regs_.f = alu_bit(regs_.e, 4, regs_.f);
        return 8;
    case 0x64: // bit 4, h
        regs_.f = alu_bit(regs_.h, 4, regs_.f);
        return 8;
    case 0x65: // bit 4, l
        regs_.f = alu_bit(regs_.l, 4, regs_.f);
        return 8;
    case 0x66: // bit 4, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 4, regs_.f);
        return 12;
    case 0x67: // bit 4, a
        regs_.f = alu_bit(regs_.a, 4, regs_.f);
        return 8;
    case 0x68: // bit 5, b
        regs_.f = alu_bit(regs_.b, 5, regs_.f);
        return 8;
    case 0x69: // bit 5, c
        regs_.f = alu_bit(regs_.c, 5, regs_.f);
        return 8;
    case 0x6A: // bit 5, d
        regs_.f = alu_bit(regs_.d, 5, regs_.f);
        return 8;
    case 0x6B: // bit 5, e
        regs_.f = alu_bit(regs_.e, 5, regs_.f);
        return 8;
    case 0x6C: // bit 5, h
        regs_.f = alu_bit(regs_.h, 5, regs_.f);
        return 8;
    case 0x6D: // bit 5, l
        regs_.f = alu_bit(regs_.l, 5, regs_.f);
        return 8;
    case 0x6E: // bit 5, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 5, regs_.f);
        return 12;
    case 0x6F: // bit 5, a
        regs_.f = alu_bit(regs_.a, 5, regs_.f);
        return 8;
    case 0x70: // bit 6, b
        regs_.f = alu_bit(regs_.b, 6, regs_.f);
        return 8;
    case 0x71: // bit 6, c
        regs_.f = alu_bit(regs_.c, 6, regs_.f);
        return 8;
    case 0x72: // bit 6, d
        regs_.f = alu_bit(regs_.d, 6, regs_.f);
        return 8;
    case 0x73: // bit 6, e
        regs_.f = alu_bit(regs_.e, 6, regs_.f);
        return 8;
    case 0x74: // bit 6, h
        regs_.f = alu_bit(regs_.h, 6, regs_.f);
        return 8;
    case 0x75: // bit 6, l
        regs_.f = alu_bit(regs_.l, 6, regs_.f);
        return 8;
    case 0x76: // bit 6, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 6, regs_.f);
        return 12;
    case 0x77: // bit 6, a
        regs_.f = alu_bit(regs_.a, 6, regs_.f);
        return 8;
    case 0x78: // bit 7, b
        regs_.f = alu_bit(regs_.b, 7, regs_.f);
        return 8;
    case 0x79: // bit 7, c
        regs_.f = alu_bit(regs_.c, 7, regs_.f);
        return 8;
    case 0x7A: // bit 7, d
        regs_.f = alu_bit(regs_.d, 7, regs_.f);
        return 8;
    case 0x7B: // bit 7, e
        regs_.f = alu_bit(regs_.e, 7, regs_.f);
        return 8;
    case 0x7C: // bit 7, h
        regs_.f = alu_bit(regs_.h, 7, regs_.f);
        return 8;
    case 0x7D: // bit 7, l
        regs_.f = alu_bit(regs_.l, 7, regs_.f);
        return 8;
    case 0x7E: // bit 7, (hl)
        regs_.f = alu_bit(bus_.read8(hl()), 7, regs_.f);
        return 12;
    case 0x7F: // bit 7, a
        regs_.f = alu_bit(regs_.a, 7, regs_.f);
        return 8;
    case 0x80: { // res 0, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 0)));
        regs_.b = v;
        return 8;
    }
    case 0x81: { // res 0, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 0)));
        regs_.c = v;
        return 8;
    }
    case 0x82: { // res 0, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 0)));
        regs_.d = v;
        return 8;
    }
    case 0x83: { // res 0, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 0)));
        regs_.e = v;
        return 8;
    }
    case 0x84: { // res 0, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 0)));
        regs_.h = v;
        return 8;
    }
    case 0x85: { // res 0, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 0)));
        regs_.l = v;
        return 8;
    }
    case 0x86: { // res 0, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 0)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0x87: { // res 0, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 0)));
        regs_.a = v;
        return 8;
    }
    case 0x88: { // res 1, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 1)));
        regs_.b = v;
        return 8;
    }
    case 0x89: { // res 1, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 1)));
        regs_.c = v;
        return 8;
    }
    case 0x8A: { // res 1, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 1)));
        regs_.d = v;
        return 8;
    }
    case 0x8B: { // res 1, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 1)));
        regs_.e = v;
        return 8;
    }
    case 0x8C: { // res 1, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 1)));
        regs_.h = v;
        return 8;
    }
    case 0x8D: { // res 1, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 1)));
        regs_.l = v;
        return 8;
    }
    case 0x8E: { // res 1, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 1)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0x8F: { // res 1, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 1)));
        regs_.a = v;
        return 8;
    }
    case 0x90: { // res 2, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 2)));
        regs_.b = v;
        return 8;
    }
    case 0x91: { // res 2, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 2)));
        regs_.c = v;
        return 8;
    }
    case 0x92: { // res 2, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 2)));
        regs_.d = v;
        return 8;
    }
    case 0x93: { // res 2, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 2)));
        regs_.e = v;
        return 8;
    }
    case 0x94: { // res 2, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 2)));
        regs_.h = v;
        return 8;
    }
    case 0x95: { // res 2, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 2)));
        regs_.l = v;
        return 8;
    }
    case 0x96: { // res 2, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 2)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0x97: { // res 2, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 2)));
        regs_.a = v;
        return 8;
    }
    case 0x98: { // res 3, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 3)));
        regs_.b = v;
        return 8;
    }
    case 0x99: { // res 3, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 3)));
        regs_.c = v;
        return 8;
    }
    case 0x9A: { // res 3, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 3)));
        regs_.d = v;
        return 8;
    }
    case 0x9B: { // res 3, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 3)));
        regs_.e = v;
        return 8;
    }
    case 0x9C: { // res 3, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 3)));
        regs_.h = v;
        return 8;
    }
    case 0x9D: { // res 3, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 3)));
        regs_.l = v;
        return 8;
    }
    case 0x9E: { // res 3, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 3)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0x9F: { // res 3, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 3)));
        regs_.a = v;
        return 8;
    }
    case 0xA0: { // res 4, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 4)));
        regs_.b = v;
        return 8;
    }
    case 0xA1: { // res 4, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 4)));
        regs_.c = v;
        return 8;
    }
    case 0xA2: { // res 4, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 4)));
        regs_.d = v;
        return 8;
    }
    case 0xA3: { // res 4, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 4)));
        regs_.e = v;
        return 8;
    }
    case 0xA4: { // res 4, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 4)));
        regs_.h = v;
        return 8;
    }
    case 0xA5: { // res 4, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 4)));
        regs_.l = v;
        return 8;
    }
    case 0xA6: { // res 4, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 4)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xA7: { // res 4, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 4)));
        regs_.a = v;
        return 8;
    }
    case 0xA8: { // res 5, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 5)));
        regs_.b = v;
        return 8;
    }
    case 0xA9: { // res 5, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 5)));
        regs_.c = v;
        return 8;
    }
    case 0xAA: { // res 5, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 5)));
        regs_.d = v;
        return 8;
    }
    case 0xAB: { // res 5, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 5)));
        regs_.e = v;
        return 8;
    }
    case 0xAC: { // res 5, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 5)));
        regs_.h = v;
        return 8;
    }
    case 0xAD: { // res 5, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 5)));
        regs_.l = v;
        return 8;
    }
    case 0xAE: { // res 5, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 5)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xAF: { // res 5, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 5)));
        regs_.a = v;
        return 8;
    }
    case 0xB0: { // res 6, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 6)));
        regs_.b = v;
        return 8;
    }
    case 0xB1: { // res 6, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 6)));
        regs_.c = v;
        return 8;
    }
    case 0xB2: { // res 6, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 6)));
        regs_.d = v;
        return 8;
    }
    case 0xB3: { // res 6, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 6)));
        regs_.e = v;
        return 8;
    }
    case 0xB4: { // res 6, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 6)));
        regs_.h = v;
        return 8;
    }
    case 0xB5: { // res 6, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 6)));
        regs_.l = v;
        return 8;
    }
    case 0xB6: { // res 6, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 6)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xB7: { // res 6, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 6)));
        regs_.a = v;
        return 8;
    }
    case 0xB8: { // res 7, b
        const uint8_t v = static_cast<uint8_t>(regs_.b & static_cast<uint8_t>(~(1u << 7)));
        regs_.b = v;
        return 8;
    }
    case 0xB9: { // res 7, c
        const uint8_t v = static_cast<uint8_t>(regs_.c & static_cast<uint8_t>(~(1u << 7)));
        regs_.c = v;
        return 8;
    }
    case 0xBA: { // res 7, d
        const uint8_t v = static_cast<uint8_t>(regs_.d & static_cast<uint8_t>(~(1u << 7)));
        regs_.d = v;
        return 8;
    }
    case 0xBB: { // res 7, e
        const uint8_t v = static_cast<uint8_t>(regs_.e & static_cast<uint8_t>(~(1u << 7)));
        regs_.e = v;
        return 8;
    }
    case 0xBC: { // res 7, h
        const uint8_t v = static_cast<uint8_t>(regs_.h & static_cast<uint8_t>(~(1u << 7)));
        regs_.h = v;
        return 8;
    }
    case 0xBD: { // res 7, l
        const uint8_t v = static_cast<uint8_t>(regs_.l & static_cast<uint8_t>(~(1u << 7)));
        regs_.l = v;
        return 8;
    }
    case 0xBE: { // res 7, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) & static_cast<uint8_t>(~(1u << 7)));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xBF: { // res 7, a
        const uint8_t v = static_cast<uint8_t>(regs_.a & static_cast<uint8_t>(~(1u << 7)));
        regs_.a = v;
        return 8;
    }
    case 0xC0: { // set 0, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 0));
        regs_.b = v;
        return 8;
    }
    case 0xC1: { // set 0, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 0));
        regs_.c = v;
        return 8;
    }
    case 0xC2: { // set 0, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 0));
        regs_.d = v;
        return 8;
    }
    case 0xC3: { // set 0, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 0));
        regs_.e = v;
        return 8;
    }
    case 0xC4: { // set 0, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 0));
        regs_.h = v;
        return 8;
    }
    case 0xC5: { // set 0, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 0));
        regs_.l = v;
        return 8;
    }
    case 0xC6: { // set 0, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 0));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xC7: { // set 0, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 0));
        regs_.a = v;
        return 8;
    }
    case 0xC8: { // set 1, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 1));
        regs_.b = v;
        return 8;
    }
    case 0xC9: { // set 1, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 1));
        regs_.c = v;
        return 8;
    }
    case 0xCA: { // set 1, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 1));
        regs_.d = v;
        return 8;
    }
    case 0xCB: { // set 1, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 1));
        regs_.e = v;
        return 8;
    }
    case 0xCC: { // set 1, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 1));
        regs_.h = v;
        return 8;
    }
    case 0xCD: { // set 1, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 1));
        regs_.l = v;
        return 8;
    }
    case 0xCE: { // set 1, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 1));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xCF: { // set 1, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 1));
        regs_.a = v;
        return 8;
    }
    case 0xD0: { // set 2, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 2));
        regs_.b = v;
        return 8;
    }
    case 0xD1: { // set 2, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 2));
        regs_.c = v;
        return 8;
    }
    case 0xD2: { // set 2, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 2));
        regs_.d = v;
        return 8;
    }
    case 0xD3: { // set 2, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 2));
        regs_.e = v;
        return 8;
    }
    case 0xD4: { // set 2, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 2));
        regs_.h = v;
        return 8;
    }
    case 0xD5: { // set 2, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 2));
        regs_.l = v;
        return 8;
    }
    case 0xD6: { // set 2, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 2));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xD7: { // set 2, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 2));
        regs_.a = v;
        return 8;
    }
    case 0xD8: { // set 3, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 3));
        regs_.b = v;
        return 8;
    }
    case 0xD9: { // set 3, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 3));
        regs_.c = v;
        return 8;
    }
    case 0xDA: { // set 3, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 3));
        regs_.d = v;
        return 8;
    }
    case 0xDB: { // set 3, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 3));
        regs_.e = v;
        return 8;
    }
    case 0xDC: { // set 3, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 3));
        regs_.h = v;
        return 8;
    }
    case 0xDD: { // set 3, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 3));
        regs_.l = v;
        return 8;
    }
    case 0xDE: { // set 3, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 3));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xDF: { // set 3, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 3));
        regs_.a = v;
        return 8;
    }
    case 0xE0: { // set 4, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 4));
        regs_.b = v;
        return 8;
    }
    case 0xE1: { // set 4, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 4));
        regs_.c = v;
        return 8;
    }
    case 0xE2: { // set 4, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 4));
        regs_.d = v;
        return 8;
    }
    case 0xE3: { // set 4, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 4));
        regs_.e = v;
        return 8;
    }
    case 0xE4: { // set 4, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 4));
        regs_.h = v;
        return 8;
    }
    case 0xE5: { // set 4, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 4));
        regs_.l = v;
        return 8;
    }
    case 0xE6: { // set 4, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 4));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xE7: { // set 4, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 4));
        regs_.a = v;
        return 8;
    }
    case 0xE8: { // set 5, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 5));
        regs_.b = v;
        return 8;
    }
    case 0xE9: { // set 5, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 5));
        regs_.c = v;
        return 8;
    }
    case 0xEA: { // set 5, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 5));
        regs_.d = v;
        return 8;
    }
    case 0xEB: { // set 5, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 5));
        regs_.e = v;
        return 8;
    }
    case 0xEC: { // set 5, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 5));
        regs_.h = v;
        return 8;
    }
    case 0xED: { // set 5, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 5));
        regs_.l = v;
        return 8;
    }
    case 0xEE: { // set 5, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 5));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xEF: { // set 5, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 5));
        regs_.a = v;
        return 8;
    }
    case 0xF0: { // set 6, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 6));
        regs_.b = v;
        return 8;
    }
    case 0xF1: { // set 6, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 6));
        regs_.c = v;
        return 8;
    }
    case 0xF2: { // set 6, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 6));
        regs_.d = v;
        return 8;
    }
    case 0xF3: { // set 6, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 6));
        regs_.e = v;
        return 8;
    }
    case 0xF4: { // set 6, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 6));
        regs_.h = v;
        return 8;
    }
    case 0xF5: { // set 6, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 6));
        regs_.l = v;
        return 8;
    }
    case 0xF6: { // set 6, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 6));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xF7: { // set 6, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 6));
        regs_.a = v;
        return 8;
    }
    case 0xF8: { // set 7, b
        const uint8_t v = static_cast<uint8_t>(regs_.b | (1u << 7));
        regs_.b = v;
        return 8;
    }
    case 0xF9: { // set 7, c
        const uint8_t v = static_cast<uint8_t>(regs_.c | (1u << 7));
        regs_.c = v;
        return 8;
    }
    case 0xFA: { // set 7, d
        const uint8_t v = static_cast<uint8_t>(regs_.d | (1u << 7));
        regs_.d = v;
        return 8;
    }
    case 0xFB: { // set 7, e
        const uint8_t v = static_cast<uint8_t>(regs_.e | (1u << 7));
        regs_.e = v;
        return 8;
    }
    case 0xFC: { // set 7, h
        const uint8_t v = static_cast<uint8_t>(regs_.h | (1u << 7));
        regs_.h = v;
        return 8;
    }
    case 0xFD: { // set 7, l
        const uint8_t v = static_cast<uint8_t>(regs_.l | (1u << 7));
        regs_.l = v;
        return 8;
    }
    case 0xFE: { // set 7, (hl)
        const uint8_t v = static_cast<uint8_t>(bus_.read8(hl()) | (1u << 7));
        bus_.write8(hl(), v);
        return 16;
    }
    case 0xFF: { // set 7, a
        const uint8_t v = static_cast<uint8_t>(regs_.a | (1u << 7));
        regs_.a = v;
        return 8;
    }
    }
    return trap_unknown(opcode);
}
