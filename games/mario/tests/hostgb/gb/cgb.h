#ifndef HOSTGB_GB_CGB_H
#define HOSTGB_GB_CGB_H

// the one gbdk declaration the generated title art needs, so the host test can compile the same
// arrays the rom does. nothing else from gbdk is, or should become, visible here
#include <stdint.h>

typedef uint16_t palette_color_t;

#endif
